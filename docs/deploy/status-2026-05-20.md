# Deployment Status 2026-05-20

## AIPPT Public Release

- Public repo: `https://github.com/mouxue56-debug/aippt`
- Public tools home: `https://mouxue56-debug.github.io/aippt/`
- Public editor direct URL: `https://mouxue56-debug.github.io/aippt/#/aippt`
- GitHub Pages workflow: `.github/workflows/pages.yml`
- Use the latest successful GitHub Actions run for the current production Pages state.

Verified locally:

```bash
npm test
npm run build
npm run package:public
VITE_AIPPT_BASE_PATH=/aippt/ npm run build:public
node scripts/prepare-github-pages.mjs
npm run smoke
```

Verified online:

- `https://mouxue56-debug.github.io/aippt/` returns HTTP 200.
- The page loads the public tools home in the in-app browser.
- `https://mouxue56-debug.github.io/aippt/#/aippt` loads the public editor.
- Public UI shows local HTML import and does not expose internal AI controls.

## fuluckai.com Tools Path

Target routes:

- `https://fuluckai.com/tools/`
- `https://fuluckai.com/tools/aippt`

Current probe result:

- Both routes return the main `fuluckai.com` homepage HTML.
- The canonical URL is still `https://fuluckai.com/`.
- This means the public AIPPT tool is not yet merged into the main site deployment.

Safe handoff package:

```txt
release/fuluckai-tools/
```

Do not deploy this package directly over the root Cloudflare Pages project unless it is merged with the existing main-site build output. A direct Pages deploy from this package would risk replacing the current homepage.

Current blocker:

- `wrangler whoami` reports not logged in, so Cloudflare Pages deployment cannot be performed from this machine/session.
- The local source directory for the current `fuluckai.com` root site was not identified in this AIPPT workspace.

Recommended next step:

1. Find or clone the current `fuluckai.com` main-site source.
2. Copy `release/fuluckai-tools/tools/` into the main site's static `/tools/` output path.
3. Add redirects/fallbacks for `/tools/aippt`.
4. Deploy the full merged main-site build through its existing Cloudflare Pages workflow.

## AIblog Check

Checked production routes:

- `https://aiblog.fuluckai.com/`
- `https://aiblog.fuluckai.com/zh`
- `https://aiblog.fuluckai.com/ja`
- `https://aiblog.fuluckai.com/en`
- `https://aiblog.fuluckai.com/zh/debate`
- `https://aiblog.fuluckai.com/zh/blog`
- `https://aiblog.fuluckai.com/daoge`
- `https://aiblog.fuluckai.com/daogeAI`
- `https://aiblog.fuluckai.com/api/debate/topics`
- `https://aiblog.fuluckai.com/api/daily-report`

Result:

- Public pages and APIs above returned HTTP 200.
- `https://aiblog.fuluckai.com/api/daily-digest?type=morning` returned HTTP 401, which is expected for a protected cron/admin-style endpoint.
- Vercel inspection shows `aiblog.fuluckai.com` belongs to project `will-ai-lab`.
- Vercel inspection shows `daoge-chat.vercel.app` belongs to project `daoge-chat`.
- `will-ai-blog/vercel.json` still proxies `/daoge`, `/daogeAI`, `/audio/presets/:path*`, and `/avatars/:path*` to `daoge-chat.vercel.app`.

Local build:

```bash
NEXT_PUBLIC_SUPABASE_URL=https://example.supabase.co \
NEXT_PUBLIC_SUPABASE_ANON_KEY=dummy \
SUPABASE_SERVICE_ROLE_KEY=dummy \
npm run build
```

This build passed. A build without required Supabase environment variables will fail locally.

Known issue:

- `npm audit --audit-level=high --omit=dev` still reports production dependency advisories, including Next.js-related high severity items.
- This should be handled as a separate AIblog dependency-upgrade task with build, route, and Vercel regression checks before deployment.
